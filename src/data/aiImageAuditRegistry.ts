import type { AIImageAuditRegistry } from "../types";
import registryData from "./aiImageAuditRegistry.json";

export const aiImageAuditRegistry: AIImageAuditRegistry = registryData as AIImageAuditRegistry;
